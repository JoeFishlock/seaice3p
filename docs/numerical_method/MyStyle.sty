\NeedsTeXFormat{LaTeX2e}[1994/06/01]
\ProvidesPackage{MyStyle}
  [Provides my usual packages]

%Alternative Setion Numbers
%\renewcommand{\thesection}{\Roman{section}.} 
%\renewcommand{\thesubsection}{\Alph{subsection}.}

%Center Section Titles
\RequirePackage[center,small,bf]{titlesec}

% Set paragraph indent and spacing
\setlength{\parindent}{1em}
%\setlength{\parskip}{6pt}

%font
\RequirePackage{lmodern}

% These change font but don't play well with the other packages
\RequirePackage[T1]{fontenc}
\RequirePackage[bitstream-charter]{mathdesign} %To use need to turn of amssymb amsmath and mathscr

%abstract
\RequirePackage{abstract}
%\renewcommand{\abstractname}{} 

\RequirePackage{float} %for better floats

\RequirePackage[margin = 1cm, bottom = 1.5cm]{geometry} %for figures

\RequirePackage{caption} %for everything 
\RequirePackage{subcaption}

\RequirePackage{afterpage} %new pages

\RequirePackage{graphicx} %for figures 
\graphicspath{ {./figures/} }
%\renewcommand{\figurename}{fig.}

%\RequirePackage{amsmath} %for equations
%\RequirePackage{amssymb} %for equations

\RequirePackage{epstopdf} %for figures

\epstopdfsetup{update}

%For derivatives, vectors, evaluation bars  etc..
%To get snippets in VScode better to add to preamble 
\RequirePackage{physics} 
\RequirePackage{esdiff}

%sub figures
%\RequirePackage{subfig}

%for multiple citations
%\RequirePackage{cite} 

%tables
\RequirePackage{booktabs}

%array environment for tables and alligning equations
\RequirePackage{array} 

%typeset SI units
\RequirePackage{siunitx}

%Physics
%\RequirePackage{physics}

%For bibliography with doi
%\RequirePackage[round]{natbib}
%\RequirePackage{url}
%\RequirePackage{doi} %this loads hyperref
%\hypersetup{colorlinks=true,linkcolor=black,anchorcolor=black,citecolor=black,filecolor=black,menucolor=black,runcolor=black,urlcolor=black}
%\RequirePackage{textcomp}
%\setcitestyle{notesep={ }}

%For references do /cref with fig: sec: eq: etc
\RequirePackage[capitalize]{cleveref}

%For math script
%\RequirePackage{mathrsfs}

%Wrap figures
\RequirePackage{wrapfig}

%Colors
\RequirePackage[dvipsnames]{xcolor}
\definecolor{BackColor}{HTML}{FFF8E7}

%Set up listing environment for code
\RequirePackage{listings}

\lstdefinestyle{mystyle}{
    backgroundcolor=\color{BackColor},   
    commentstyle=\color{ForestGreen},
    keywordstyle=\color{MidnightBlue},
    numberstyle=\tiny\color{Gray},
    stringstyle=\color{OrangeRed},
    basicstyle=\ttfamily\footnotesize,
    breakatwhitespace=false,         
    breaklines=true,                 
    captionpos=b,                    
    keepspaces=true,                 
    numbers=left,                    
    numbersep=5pt,                  
    showspaces=false,                
    showstringspaces=false,
    showtabs=false,                  
    tabsize=2
}

\lstset{style=mystyle}

%For Textboxes
\usepackage[most]{tcolorbox}
%My Colour Palette
\definecolor{king1}{HTML}{15209C}
\definecolor{king2}{HTML}{B3751F}

\RequirePackage{mymacros}

\endinput
